# Render API Configuration
# Replace with your actual Render API URL - this is the ONLY environment variable needed!
# All other configuration (Neon DB credentials, API keys, etc.) is managed by Render
VITE_RENDER_API_URL=https://your-render-app.onrender.com

# Optional: API Logging (set to 'true' for development, 'false' for production)
VITE_ENABLE_API_LOGGING=true