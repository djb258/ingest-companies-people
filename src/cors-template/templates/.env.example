# Render API Configuration
# Replace with your actual Render API URL
VITE_API_URL=https://your-render-app.onrender.com

# API Logging (set to 'true' for development, 'false' for production)
VITE_ENABLE_API_LOGGING=true

# Optional: Custom timeout in milliseconds
VITE_API_TIMEOUT=30000

# Optional: Enable retry on network errors
VITE_API_RETRY_ATTEMPTS=3

# Optional: Custom domain for CORS (if using custom domain)
VITE_CUSTOM_DOMAIN=https://your-custom-domain.com